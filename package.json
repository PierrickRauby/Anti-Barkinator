{
  "name": "barking-project-app",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "nodemon server.js --ignore './client/'",
    "dev": "concurrently --kill-others-on-fail \"node server.js\" \"npm run client\""
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "core-util-is": "^1.0.2",
    "express": "^4.17.1",
    "moment": "^2.29.1",
    "nodemon": "^2.0.6",
    "react-script": "^2.0.5"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
